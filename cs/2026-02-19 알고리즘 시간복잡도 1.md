자료구조+알고리즘 ---->코드(c,java,python)[이러한 코드들은 실제 컴퓨터 안에서 작동] 그러나 HW/SW 환경 상이(!) 또한 다양한 크기의 입력

가상 컴퓨터(Virtual Machine)
    ->위에 이유로 HW/SW 환경에 영향을 받지 않은 컴퓨터.
+가상언어(pseudo Language) + 가상 코드(pseudo code)

가상 컴퓨터:Turing Machine ---> von Neumann : RAM(Randon Acess Machine)
RAM + cpu + memory + 기본 연산[단위시간에 수행되는 연산]
기본 연산:
    -배정,대입,복사연산 ->소요 1시간
    -산술연산 ->소요 1시간
    -비교연산 ->소요 1시간
    -논리연산 ->소요 1시간
    -비트연산 ->소요 1시간

가상언어
    -배정,산술,비교,논리,bit-논란
    -비교: if elif else
    -반복:for while
    -함수:정의,호출,return
  
  
  
  algorithm ArrayMax(A,n):->입력
  input:n개의 정수를 갖는 배열 A
  output:A의 수중에서 최댓값 리턴
  currentMax=A[0]
  for i = 1 to n-1 do
      if CurrentMax=A[i]
          CurentMax=A[i]
return CurrentMax

만약 입력 n=2, A=[3,-1,9,2,12]라면 위에서 반복문, 기본연산이 7회가 된다.

 그러나 무한히 많은 입력 크기 n을 매번 기본연산 횟수를 적을 수 없다.
 그러므로 우리는 이러한 문제를 다음에 살펴 볼것이다
 